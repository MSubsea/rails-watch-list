<div class="container">
<h1>Add a new review</h1>
<%= simple_form_for [ @list, @vote ] do |f|%>
<%= f.input :content %>
<%= f.input :rating %>
<%= f.input :list_id, :as => :hidden, :input_html => { :value => @list.id } %>
<%= f.submit %>
<% end %>
</div>
